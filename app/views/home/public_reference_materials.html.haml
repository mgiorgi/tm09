%h1
  Material Público
%br
%table
  = render :partial => 'shared/reference_material', :collection => @reference_materials, :locals => { :redirect_path => url_for({:controller => 'home', :action => 'public_reference_materials' }) }
